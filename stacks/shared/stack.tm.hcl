stack {
  name        = "shared"
  description = "Resources every Valheim server uses: firewall rules and the start-instance custom role."
  id          = "valheim-shared"
  tags        = ["valheim"]
}
